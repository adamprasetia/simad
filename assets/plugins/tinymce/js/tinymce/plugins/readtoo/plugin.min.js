tinymce.PluginManager.add('readtoo', function(editor, url) {
    editor.addButton('readtoo', {
        title: 'Baca Juga',
        icon: 'browse',
        onclick: function() {
            var title = 'Read too';
            var src = base_url + module + '/index/2?popup=1&module=readtoo';
            $('#general-modal-title').html(title);
            $('#general-modal-iframe').attr('src',src);
            $('#general-modal').modal('show');
        }
    });
});